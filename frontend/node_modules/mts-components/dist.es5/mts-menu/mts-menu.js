import * as tslib_1 from "tslib";
import { MTSBase } from "../core/mts-base";
import { draw } from "./mts-menu.view";
import style, * as style_cjs from "./mts-menu.scss";
export var MTSMenuHorizontalPosition;
(function (MTSMenuHorizontalPosition) {
    MTSMenuHorizontalPosition["left"] = "left";
    MTSMenuHorizontalPosition["center"] = "center";
    MTSMenuHorizontalPosition["right"] = "right";
})(MTSMenuHorizontalPosition || (MTSMenuHorizontalPosition = {}));
export var MTSMenuVerticalPosition;
(function (MTSMenuVerticalPosition) {
    MTSMenuVerticalPosition["top"] = "top";
    MTSMenuVerticalPosition["middle"] = "middle";
    MTSMenuVerticalPosition["bottom"] = "bottom";
})(MTSMenuVerticalPosition || (MTSMenuVerticalPosition = {}));
var MTSMenu = /** @class */ (function (_super) {
    tslib_1.__extends(MTSMenu, _super);
    function MTSMenu(html, css) {
        var _this = _super.call(this, html || draw, css || style || style_cjs) || this;
        _this._open = false;
        _this._isAnimationProgress = false;
        _this._anchorEl = null;
        _this._anchorOrigin = null;
        _this._anchorPosition = "0 0";
        _this._direction = "center bottom";
        _this.startPoint = {
            top: "0",
            left: "0",
        };
        // back setup
        _this._backElement.addEventListener("click", function (event) {
            _this.dispatchComponentsEvent("close", event);
            event.stopPropagation();
            event.preventDefault();
        });
        // menu setup
        _this._menuElement.addEventListener("animationend", function () {
            _this._isAnimationProgress = false;
            _this._menuElement.style.display = _this._open ? "inline-block" : "none";
        });
        _this.slotElement = _this.shadowRoot.querySelector("slot");
        return _this;
    }
    Object.defineProperty(MTSMenu, "observedAttributes", {
        get: function () {
            return ["open", "anchor-el", "anchor-origin", "anchor-position", "direction"];
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(MTSMenu.prototype, "open", {
        get: function () {
            return this._open;
        },
        set: function (value) {
            var unkValue = value;
            this._open = unkValue === "" || unkValue === "open" || unkValue === "true" || unkValue === true;
            this.processOpen();
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(MTSMenu.prototype, "anchorEl", {
        get: function () {
            return this._anchorEl;
        },
        set: function (value) {
            var unkValue = value;
            if (unkValue instanceof HTMLElement) {
                this._anchorEl = unkValue;
            }
            else if (typeof unkValue === "string") {
                var elem = document.querySelector("#{value}");
                this._anchorEl = elem instanceof HTMLElement ? elem : null;
            }
            else {
                this._anchorEl = null;
            }
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(MTSMenu.prototype, "direction", {
        get: function () {
            return this._direction;
        },
        set: function (value) {
            this._direction = this.filterParams(value);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(MTSMenu.prototype, "anchorOrigin", {
        get: function () {
            return this._anchorOrigin;
        },
        set: function (value) {
            this._anchorOrigin = value ? this.filterParams(value) : null;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(MTSMenu.prototype, "anchorPosition", {
        get: function () {
            return this._anchorPosition;
        },
        set: function (value) {
            this._anchorPosition = value ? String(value) : "0 0";
        },
        enumerable: true,
        configurable: true
    });
    MTSMenu.prototype.filterParams = function (value) {
        var inValue = String(value);
        var horPosition = Object
            .values(MTSMenuHorizontalPosition)
            .filter(function (pos) { return inValue.includes(pos); })[0] || MTSMenuHorizontalPosition.left;
        var vertPosition = Object
            .values(MTSMenuVerticalPosition)
            .filter(function (pos) { return inValue.includes(pos); })[0] || MTSMenuVerticalPosition.bottom;
        return horPosition + " " + vertPosition;
    };
    // tslint:disable-next-line:cyclomatic-complexity
    MTSMenu.prototype.processOpen = function () {
        var _this = this;
        var directions = this._direction.split(" ");
        if (this._open) {
            var anchorElement = this.getAnchorElement();
            if (anchorElement) {
                this.startPoint = this.getStartPoint();
                this._menuElement.style.top = this.startPoint.top;
                this._menuElement.style.left = this.startPoint.left;
            }
            this._backElement.style.display = "inline-block";
            var backComputed = this._backElement.getBoundingClientRect();
            this._backElement.style.left = -1 * backComputed.left + "px";
            this._backElement.style.top = -1 * backComputed.top + "px";
            this._menuElement.style.display = "inline-block";
            this._menuElement.className = tslib_1.__spread(["menu open"], directions).join("--");
            this._isAnimationProgress = true;
            requestAnimationFrame(function () { return _this.checkScreenBoundingOffset(); });
        }
        else {
            this._backElement.style.display = "none";
            this._backElement.style.left = "0px";
            this._backElement.style.top = "0px";
            this._menuElement.className = tslib_1.__spread(["menu close"], directions).join("--");
            this._isAnimationProgress = true;
        }
    };
    MTSMenu.prototype.checkScreenBoundingOffset = function () {
        var _this = this;
        var menuBounding = this._menuElement.getBoundingClientRect();
        var top = Number.parseFloat(this._menuElement.style.top || "0");
        var left = Number.parseFloat(this._menuElement.style.left || "0");
        var dy = 0;
        var dx = 0;
        // top bounding
        if (menuBounding.top < 0) {
            dy = -menuBounding.top;
        }
        // right bounding
        if (menuBounding.right > window.innerWidth) {
            dx = window.innerWidth - menuBounding.right;
        }
        // bottom bounding
        if (menuBounding.bottom > window.innerHeight) {
            dy = window.innerHeight - menuBounding.bottom;
        }
        // left bounding
        if (menuBounding.left < 0) {
            dx = -menuBounding.left;
        }
        this._menuElement.style.top = top + dy + "px";
        this._menuElement.style.left = left + dx + "px";
        dy = 0;
        dx = 0;
        var anchorElement = this.getAnchorElement();
        var anchorBounding = anchorElement ? anchorElement.getBoundingClientRect() : null;
        if (anchorBounding) {
            var newMenuBounding = this._menuElement.getBoundingClientRect();
            // top anchor stickiness
            if (newMenuBounding.top > anchorBounding.bottom) {
                dy = anchorBounding.bottom - newMenuBounding.top;
                // bottom anchor stickiness
            }
            else if (newMenuBounding.bottom < anchorBounding.top) {
                dy = anchorBounding.top - newMenuBounding.bottom;
            }
            // left anchor stickiness
            if (newMenuBounding.left > anchorBounding.right) {
                dx = anchorBounding.right - newMenuBounding.left;
                // right anchor stickiness
            }
            else if (newMenuBounding.right < anchorBounding.left) {
                dx = anchorBounding.left - newMenuBounding.right;
            }
        }
        top = Number.parseFloat(this._menuElement.style.top || "0");
        left = Number.parseFloat(this._menuElement.style.left || "0");
        this._menuElement.style.top = top + dy + "px";
        this._menuElement.style.left = left + dx + "px";
        if (this._isAnimationProgress) {
            requestAnimationFrame(function () { return _this.checkScreenBoundingOffset(); });
        }
    };
    MTSMenu.prototype.getAnchorElement = function () {
        return this._anchorEl || this.previousElementSibling;
    };
    MTSMenu.prototype.getStartPoint = function () {
        var anchorElement = this.getAnchorElement();
        if (!anchorElement) {
            return {
                top: "0",
                left: "0",
            };
        }
        var top = 0;
        var left = 0;
        var anchorOrigin = (this._anchorOrigin || this._direction).split(" ");
        var anchorBounding = anchorElement.getBoundingClientRect();
        var thisBounding = this.getBoundingClientRect();
        var deltaLeft = anchorBounding.left - thisBounding.left;
        if (anchorOrigin[0] === MTSMenuHorizontalPosition.left) {
            left = deltaLeft;
        }
        else if (anchorOrigin[0] === MTSMenuHorizontalPosition.center) {
            left = deltaLeft + anchorBounding.width / 2;
        }
        else if (anchorOrigin[0] === MTSMenuHorizontalPosition.right) {
            left = deltaLeft + anchorBounding.width;
        }
        var deltaTop = anchorBounding.top - thisBounding.top;
        if (anchorOrigin[1] === MTSMenuVerticalPosition.top) {
            top = deltaTop;
        }
        else if (anchorOrigin[1] === MTSMenuVerticalPosition.middle) {
            top = deltaTop + anchorBounding.height / 2;
        }
        else if (anchorOrigin[1] === MTSMenuVerticalPosition.bottom) {
            top = deltaTop + anchorBounding.height;
        }
        return {
            top: top + "px",
            left: left + "px",
        };
    };
    MTSMenu.selector = "mts-menu";
    return MTSMenu;
}(MTSBase));
export { MTSMenu };
//# sourceMappingURL=mts-menu.js.map