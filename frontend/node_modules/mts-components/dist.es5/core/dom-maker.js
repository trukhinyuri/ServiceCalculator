var svgElements = [
    "animate", "circle", "clipPath", "defs", "desc", "ellipse", "feBlend", "feColorMatrix", "feComponentTransfer", "feComposite",
    "feConvolveMatrix", "feDiffuseLighting", "feDisplacementMap", "feDistantLight", "feDropShadow", "feFlood", "feFuncA", "feFuncB",
    "feFuncG", "feFuncR", "feGaussianBlur", "feImage", "feMerge", "feMergeNode", "feMorphology", "feOffset", "fePointLight",
    "feSpecularLighting", "feSpotLight", "feTile", "feTurbulence", "filter", "foreignObject", "g", "image", "line", "linearGradient",
    "marker", "mask", "metadata", "path", "pattern", "polygon", "polyline", "radialGradient", "rect", "stop", "svg", "switch", "symbol",
    "text", "textPath", "tspan", "use", "view"
];
export function make(tagName, props) {
    var childrens = [];
    for (var _i = 2; _i < arguments.length; _i++) {
        childrens[_i - 2] = arguments[_i];
    }
    // tag
    var isSvg = svgElements.includes(tagName);
    var elem = isSvg ?
        document.createElementNS("http://www.w3.org/2000/svg", tagName) :
        document.createElement(tagName);
    // props
    if (props) {
        var keys = Reflect.ownKeys(props);
        keys.forEach(function (key) {
            var value = Reflect.get(props, key);
            if (key === "ref" && value instanceof Function) {
                value(elem);
            }
            else if (key === "style" && typeof value === "object" && value !== null) {
                var styles = Reflect.ownKeys(value);
                styles.forEach(function (style) {
                    Reflect.set(elem.style, style, Reflect.get(value, style));
                });
            }
            else if (isSvg && typeof key === "string" && typeof value === "string") {
                elem.setAttribute(key, value);
            }
            else if (typeof key === "string" && key.indexOf("on") === 0 && value instanceof Function) {
                elem.addEventListener(key.substring(2).toLowerCase(), value);
            }
            else {
                Reflect.set(elem, key, value);
            }
        });
    }
    // children
    if (childrens) {
        appendChildren(elem, childrens);
    }
    return elem;
}
function appendChildren(elem, childrens) {
    childrens.forEach(function (chieldren) {
        if (Array.isArray(chieldren)) {
            appendChildren(elem, chieldren);
        }
        else if (chieldren instanceof Node) {
            elem.appendChild(chieldren);
        }
        else if (typeof chieldren === "string") {
            var textNode = document.createTextNode(chieldren);
            elem.appendChild(textNode);
        }
        else if (typeof chieldren === "number") {
            var textNode = document.createTextNode(String(chieldren));
            elem.appendChild(textNode);
        }
    });
}
//# sourceMappingURL=dom-maker.js.map