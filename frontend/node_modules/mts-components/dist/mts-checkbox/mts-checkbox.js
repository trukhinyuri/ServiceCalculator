import { MTSInputBase } from "../core/mts-input-base";
import { draw } from "./mts-checkbox.view";
import style, * as style_cjs from "./mts-checkbox.scss";
export var MTSCheckboxColors;
(function (MTSCheckboxColors) {
    MTSCheckboxColors["primary"] = "primary";
    MTSCheckboxColors["secondary"] = "secondary";
    MTSCheckboxColors["tertiary"] = "tertiary";
    MTSCheckboxColors["quaternary"] = "quaternary";
    MTSCheckboxColors["default"] = "default";
})(MTSCheckboxColors || (MTSCheckboxColors = {}));
export class MTSCheckbox extends MTSInputBase {
    constructor(html, css) {
        super(html || draw, css || style || style_cjs);
        this._helperElement = document.createElement("div"); // todo: think about it
        this._checkedIcon = "";
        this._color = MTSCheckboxColors.default;
        this._indeterminate = false;
        this._inputElement.addEventListener("change", (event) => this.onChangeInput(event));
    }
    static get observedAttributes() {
        return [
            ...MTSInputBase.observedAttributes,
            "checked", "checked-icon", "color", "indeterminate"
        ];
    }
    get checked() {
        return this._inputElement.checked;
    }
    set checked(value) {
        const unkValue = value;
        this._inputElement.checked = unkValue === "" || unkValue === "checked" || unkValue === "true" || unkValue === true;
    }
    get checkedIcon() {
        return this._checkedIcon;
    }
    set checkedIcon(value) {
        const unkValue = value;
        this._checkedIcon = String(unkValue); // todo: realize it later
    }
    get color() {
        return this._color;
    }
    set color(value) {
        const unkValue = value;
        this._color = Object.values(MTSCheckboxColors).includes(unkValue) ?
            unkValue : MTSCheckboxColors.default;
        this.updateLabelStyle();
    }
    get indeterminate() {
        return this._indeterminate;
    }
    set indeterminate(value) {
        const unkValue = value;
        this._indeterminate = unkValue === "" || unkValue === "indeterminate" || unkValue === "true" || unkValue === true;
    }
    onChangeInput(event) {
        this.dispatchComponentsEvent("change", event);
    }
    updateLabelStyle() {
        const classes = ["control"];
        // todo: add colors
        this._labelElement.className = classes.join("--");
    }
}
MTSCheckbox.selector = "mts-checkbox";
//# sourceMappingURL=mts-checkbox.js.map