import { MTSBase } from "mts-components/dist.es5/core/mts-base";
import { draw } from "mts-components/dist.es5/mts-card/mts-card-media/mts-card-media.view";
import style, * as style_cjs from "mts-components/dist.es5/mts-card/mts-card-media/mts-card-media.scss";
export class MTSCardMedia extends MTSBase {
    constructor(html, css) {
        super(html || draw, css || style || style_cjs);
        this._imageSrc = null;
    }
    static get observedAttributes() {
        return ["height", "image", "title"];
    }
    get height() {
        return this._imageElement.style.height;
    }
    set height(value) {
        const unkValue = value;
        this._imageElement.style.height = unkValue ? String(unkValue) : null;
    }
    get image() {
        return this._imageSrc;
    }
    set image(value) {
        const unkValue = value;
        this._imageSrc = unkValue ? `url(${String(unkValue)})` : null;
        this._imageElement.style.backgroundImage = this._imageSrc;
    }
    get title() {
        return this._imageElement.title;
    }
    set title(value) {
        const unkValue = value;
        this._imageElement.title = unkValue ? String(unkValue) : "";
    }
}
MTSCardMedia.selector = "mts-card-media";
//# sourceMappingURL=mts-card-media.js.map