import { MTSBase } from "../core/mts-base";
import { draw } from "./mts-badge.view";
import style, * as style_cjs from "./mts-badge.scss";
export var MTSBadgeColors;
(function (MTSBadgeColors) {
    MTSBadgeColors["primary"] = "primary";
    MTSBadgeColors["secondary"] = "secondary";
    MTSBadgeColors["tertiary"] = "tertiary";
    MTSBadgeColors["quaternary"] = "quaternary";
    MTSBadgeColors["white"] = "white";
    MTSBadgeColors["grey"] = "grey";
    MTSBadgeColors["default"] = "default";
})(MTSBadgeColors || (MTSBadgeColors = {}));
export class MTSBadge extends MTSBase {
    constructor(html, css) {
        super(html || draw, css || style || style_cjs);
        this._color = MTSBadgeColors.default;
        this._size = 1;
    }
    static get observedAttributes() {
        return [
            "badge-content", "color", "size", "offset"
        ];
    }
    get color() {
        return this._color;
    }
    set color(value) {
        const unkValue = value;
        this._color = Object.values(MTSBadgeColors).includes(unkValue) ?
            unkValue : MTSBadgeColors.default;
        this.updateClassName();
    }
    get badgeContent() {
        return this._spanElement.innerHTML;
    }
    set badgeContent(value) {
        this._spanElement.innerHTML = String(value);
    }
    get size() {
        return this._size;
    }
    set size(value) {
        this._size = Number.parseFloat(String(value)) || 1;
        this._spanElement.style.fontSize = `${this._size}em`;
    }
    get offset() {
        return `${this._spanElement.style.top} ${this._spanElement.style.right}`;
    }
    set offset(value) {
        const offset = String(value).split(" ");
        this._spanElement.style.top = offset[0];
        this._spanElement.style.right = offset.length > 1 ? offset[1] : offset[0];
    }
    updateClassName() {
        const classes = [this.constructor.selector];
        if (this._color !== MTSBadgeColors.default) {
            classes.push(this._color);
        }
        this.shadowRoot.lastElementChild.className = classes.join("--");
    }
}
MTSBadge.selector = "mts-badge";
//# sourceMappingURL=mts-badge.js.map