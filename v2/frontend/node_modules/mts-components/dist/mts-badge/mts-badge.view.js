import { make } from "../core/dom-maker";
export function draw(ctrl) {
    return (make("section", null,
        make("span", { ref: (elem) => ctrl._spanElement = elem }),
        make("slot", null)));
}
//# sourceMappingURL=mts-badge.view.js.map