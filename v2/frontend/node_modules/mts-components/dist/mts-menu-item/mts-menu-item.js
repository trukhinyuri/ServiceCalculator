import { MTSBase } from "mts-components/dist.es5/core/mts-base";
import { draw } from "mts-components/dist.es5/mts-menu-item/mts-menu-item.view";
import style, * as style_cjs from "mts-components/dist.es5/mts-menu-item/mts-menu-item.scss";
export class MTSMenuItem extends MTSBase {
    constructor(html, css) {
        super(html || draw, css || style || style_cjs);
        this._focused = false;
        this._sectionElement = this.shadowRoot.lastElementChild;
    }
    static get observedAttributes() {
        return ["value"];
    }
    get value() {
        return this._value;
    }
    set value(value) {
        this._value = value;
    }
    get focused() {
        return this._focused;
    }
    set focused(focused) {
        this._focused = focused;
        if (focused) {
            this._sectionElement.classList.add(`${MTSMenuItem.selector}--focused`);
        }
        else {
            this._sectionElement.classList.remove(`${MTSMenuItem.selector}--focused`);
        }
    }
}
MTSMenuItem.selector = "mts-menu-item";
//# sourceMappingURL=mts-menu-item.js.map