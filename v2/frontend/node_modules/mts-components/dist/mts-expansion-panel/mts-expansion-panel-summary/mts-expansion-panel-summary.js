import { MTSBase } from "mts-components/dist.es5/core/mts-base";
import { draw } from "mts-components/dist.es5/mts-expansion-panel/mts-expansion-panel-summary/mts-expansion-panel-summary.view";
import style, * as style_cjs from "mts-components/dist.es5/mts-expansion-panel/mts-expansion-panel-summary/mts-expansion-panel-summary.scss";
export class MTSExpansionPanelSummary extends MTSBase {
    constructor(html, css) {
        super(html || draw, css || style || style_cjs);
        this._expandIcon = "";
        // tslint:disable-next-line:max-line-length
        this._defaultIcon = `<svg xmlns="http://www.w3.org/2000/svg" width="1.2em" height="1.2em" viewBox="4 4 16 16"><path d="M7.41 8.59L12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z"/><path fill="none" d="M0 0h24v24H0V0z"/></svg>`;
        this._opened = false;
        this._disabled = false;
        this.expandIcon = String(this._defaultIcon);
    }
    static get observedAttributes() {
        return ["expand-icon", "opened", "disabled"];
    }
    get expandIcon() {
        return this._expandIcon;
    }
    set expandIcon(value) {
        const unkValue = value;
        this._expandIcon = unkValue ? String(unkValue) : String(this._defaultIcon);
        this._expandIconElement.innerHTML = this._expandIcon;
    }
    get opened() {
        return this._opened;
    }
    set opened(value) {
        const unkValue = value;
        this._opened = unkValue === "" || unkValue === "opened" || unkValue === "true" || unkValue === true;
        this.updateStyles();
    }
    get disabled() {
        return this._disabled;
    }
    set disabled(value) {
        const unkValue = value;
        this._disabled = unkValue === "" || unkValue === "disabled" || unkValue === "true" || unkValue === true;
        this.updateStyles();
    }
    updateStyles() {
        const classes = [this.constructor.selector];
        if (this.disabled) {
            classes.push("disabled");
        }
        if (this._opened) {
            classes.push("opened");
        }
        this._sectionElement.className = classes.join("--");
    }
}
MTSExpansionPanelSummary.selector = "mts-expansion-panel-summary";
//# sourceMappingURL=mts-expansion-panel-summary.js.map