import { MTSBase } from "mts-components/dist.es5/core/mts-base";
import { draw } from "mts-components/dist.es5/mts-expansion-panel/mts-expansion-panel-actions/mts-expansion-panel-actions.view";
import style, * as style_cjs from "mts-components/dist.es5/mts-expansion-panel/mts-expansion-panel-actions/mts-expansion-panel-actions.scss";
export class MTSExpansionPanelActions extends MTSBase {
    constructor(html, css) {
        super(html || draw, css || style || style_cjs);
        this._opened = false;
        this._sectionElement.addEventListener("transitionend", () => this.setHeightInEndTransition());
        this.updateStyles();
    }
    static get observedAttributes() {
        return ["opened"];
    }
    get opened() {
        return this._opened;
    }
    set opened(value) {
        const unkValue = value;
        this._opened = unkValue === "" || unkValue === "opened" || unkValue === "true" || unkValue === true;
        this.updateStyles();
    }
    setHeightInEndTransition() {
        if (this._opened) {
            this._sectionElement.style.height = "auto";
        }
    }
    updateStyles() {
        if (this._sectionElement.scrollHeight === 0 && this._opened) {
            this._sectionElement.style.padding = "1em 2em"; // see less styles
            this._sectionElement.style.height = "auto";
        }
        else {
            const fontSize = Number.parseFloat(getComputedStyle(this._sectionElement).fontSize || "0"); // for calculate 1 em size in pixels
            // change auto to px, because animation don't work with auto
            this._sectionElement.style.height = !this._opened ? `${this._sectionElement.scrollHeight - 2 * fontSize}px` : "0";
            // second for fix chrome bug
            this._sectionElement.style.height = !this._opened ? `${this._sectionElement.scrollHeight - 2 * fontSize}px` : "0";
            this._sectionElement.style.padding = this._opened ? `1em 2em` : "0 2em"; // see less styles
            this._sectionElement.style.height = this._opened ? `${this._sectionElement.scrollHeight}px` : "0";
        }
    }
}
MTSExpansionPanelActions.selector = "mts-expansion-panel-actions";
//# sourceMappingURL=mts-expansion-panel-actions.js.map